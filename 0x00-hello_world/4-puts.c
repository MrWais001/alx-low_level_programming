#include <stdio .h>
/**
 * main- Serves as the entry point for the program
 * Return: By default return zero if no errors otherwise return non-zero value.
 */
int main(viod)
{
 puts("\"Programming is like building a multilingual puzzle");
 return (0);
}
